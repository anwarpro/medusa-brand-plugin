import {RouteFocusModal} from "../../../components/modals";
import {CreateBrandForm} from "./create-brand-form.tsx";

const BrandCreate = () => {
    return (
        <RouteFocusModal>
            <CreateBrandForm/>
        </RouteFocusModal>
    )
}

export default BrandCreate
